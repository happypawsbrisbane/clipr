# PetSitter Pro — local environment template. Copy to .env.local for the
# frontend (Vite) and .env for Prisma. Never commit real secrets.

# Prisma (SQLite for local dev; use a postgresql:// URL for staging/prod)
DATABASE_URL="file:./dev.db"

# TODO(future): backend integrations (not used by the v1 dashboard)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# CLERK_SECRET_KEY=
